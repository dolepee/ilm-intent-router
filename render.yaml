services:
  - type: web
    name: ilm-intent-router-api
    env: node
    buildCommand: cd backend && npm ci
    startCommand: cd backend && npm run serve
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 20
