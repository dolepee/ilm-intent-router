services:
  - type: web
    name: ilm-intent-router-api
    env: node
    rootDir: backend
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 20
