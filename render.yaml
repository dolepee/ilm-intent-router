services:
  - type: web
    name: ilm-intent-router-api
    env: node
    buildCommand: cd backend && npm ci && npm run build
    startCommand: cd backend && npm run start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 20
